# 01 — 当前完成内容与测试指南

> 更新：2026-07-03  
> 仓库：`HYmd`（本仓库）+ `hymd-code`（VS Code fork，见 [hymd-code/Doc/01](../hymd-code/Doc/01-为何拆分HYmd与hymd-code.md)）

---

## 一、已完成什么

### M0 — 格式与 Parser（✅）

| 交付物 | 路径 | 能力 |
|---|---|---|
| 格式规范 v0.1 | `spec/hymd-spec-v0.1.md` | frontmatter、sheet/slide/layout/calc 围栏块、assets 约定 |
| JSON Schema | `spec/schemas/` | frontmatter、各块体 schema |
| Parser | `packages/hymd-parser` | `parseHymd` / `serializeHymd`、块注册表、往返等价 |
| 样例 | `samples/*.hy.md` | 3 个样例（纯 prose / 含 sheet / 含 slide） |
| 降级测试 | `packages/hymd-parser/test/degradation.test.ts` | 5 个主流渲染器将扩展块降级为 `<pre><code>` |

### M1 — WYSIWYG 扩展（✅）

| 交付物 | 路径 | 能力 |
|---|---|---|
| VS Code 扩展 | `packages/hymd-vscode` | `*.hy.md` CustomTextEditor，Milkdown Crepe WYSIWYG |
| 双向同步 | `packages/hymd-vscode/src/sync/` | 220ms debounce、IME 中文输入缓冲、回声过滤 |
| 块卡片 | `packages/hymd-vscode/src-webview/blockCards.ts` | sheet/slide/layout/calc 徽章样式 |
| Fork 同步 | `scripts/sync-to-fork.mjs` | 构建产物 → `../hymd-code/extensions/hymd-editor` |
| HYmd Code | `../hymd-code` | Code-OSS 1.99.3 fork，内置 hymd-editor |

### M1 — Sheet 块 + Univer（✅）

| 交付物 | 路径 | 能力 |
|---|---|---|
| Univer 封装 | `packages/hymd-blocks-sheet` | `mountSheetPreview`（只读）、`mountSheetEditor`（编辑） |
| Snapshot 工具 | `packages/hymd-blocks-sheet/src/snapshotUtils.ts` | 空表生成、公式单元格提取 |
| Parser sheet API | `packages/hymd-parser/src/sheet.ts` | `resolveSheetSource`（external / inline / empty） |
| 块体变更 | `packages/hymd-parser/src/mutate.ts` | `updateBlockBody` |
| 文档内预览 | webview `sheetPreview.ts` | IntersectionObserver 懒挂载只读 Univer |
| 全屏编辑 | webview `sheetOverlay.ts` | 「编辑」→ 覆盖层 → 保存写回 assets |
| embed/export 命令 | `hymd.sheet.embedSnapshot` / `exportSnapshot` | 外置 JSON ↔ 块体内嵌 `data` |
| 样例含公式 | `samples/02-report-with-sheet.hy.assets/load-table.univer.json` | `f: "=SUM(...)"` 等 |

### M2 — 独立编辑器壳 hymd-host（✅）

| 交付物 | 路径 | 能力 |
|---|---|---|
| 共享 webview 核心 | `packages/hymd-webview-core` | Crepe + 块卡片 + Sheet 预览/覆盖层；`createHymdEditor` |
| Web 前端 | `packages/hymd-host-web` | WebView2 用 `host.js`；`sheetMode: client` + readFile/writeFile |
| WPF 宿主 | `apps/hymd-host` | 新建/打开/保存/另存为、脏标记、关闭提示、FileSystemWatcher 外部刷新 |
| 构建 | `npm run build:host` | 先构建 host-web，再 `dotnet build apps/hymd-host` |

### 尚未完成（不在本文范围）

- M3+：layout 排版引擎、slide 块 Marp 交互、导出 docx/pptx
- HYmd Code 完整安装包分发（当前以 dev 构建 `scripts/code.bat` 为主）

---

## 二、环境要求

| 项 | 要求 |
|---|---|
| Node | **20.18.2**（HYmd / hymd-code 均推荐；见 `hymd-code/.nvmrc`） |
| 包管理 | 根目录 `.npmrc` 已配 npmmirror |
| HYmd Code 构建 | VS 2022 + Spectre 库（仅 fork 全量编译需要） |

```powershell
node -v          # 应 v20.18.2
cd HYmd
npm install
```

---

## 三、自动化测试（无需 UI）

在 **HYmd 根目录**执行：

```powershell
cd E:\BaiduSyncdisk\Code\CSharp\CursorProjects\HYmd

npm run build    # 编译 parser + blocks-sheet + webview-core + host-web + 扩展
npm test         # 60 项（blocks-sheet 4 + parser 33 + webview-core 3 + vscode 24）
```

| 包 | 测什么 |
|---|---|
| `@hymd/blocks-sheet` | snapshot 工具、公式提取 |
| `@hymd/parser` | 解析、往返、降级、sheet embed/export、公式不丢 |
| `hymd-editor` | 文档同步状态机、sheet 块协议、embed/export 往返 |

**通过标准**：全部 `Tests passed`，0 error。

---

## 四、手动测试 — 路径 A：F5（推荐，最快）

不构建整个 HYmd Code，只测扩展 + Sheet 交互。

### 步骤

```powershell
cd E:\BaiduSyncdisk\Code\CSharp\CursorProjects\HYmd
npm run build:vscode
```

1. 用 **Cursor / VS Code** 打开 **HYmd 仓库**（不是 hymd-code）
2. 按 **F5** → 启动「Extension Development Host」
3. 在新窗口打开：`samples/02-report-with-sheet.hy.md`
4. 应自动进入 **HyMD WYSIWYG**（若提示选编辑器，选 HyMD WYSIWYG）

### 验收清单

| # | 操作 | 预期 |
|---|---|---|
| 1 | 查看正文 | 标题、列表、GFM 表格正常 WYSIWYG 渲染 |
| 2 | 查看 sheet 块卡片 | 绿色边框卡片；**只读 Univer 表格**（懒加载，滚动到可见区域后显示） |
| 3 | 卡片上显示 | 路径 `./02-report-with-sheet.hy.assets/load-table.univer.json`、「编辑」按钮 |
| 4 | 点击「编辑」 | 全屏覆盖层；完整 Univer 工具栏；可改单元格 |
| 5 | 改一个数值或公式 → **保存** | 覆盖层关闭；预览刷新 |
| 6 | 打开 assets JSON | `samples/02-report-with-sheet.hy.assets/load-table.univer.json` 中 **`f` 公式字段仍在** |
| 7 | 关闭再 reopen 该文件 | 修改与公式仍保留 |
| 8 | 中文 IME | 在正文区中文输入无丢字、无整篇闪回 |

### embed / export 命令

在 **Extension Development Host** 中，打开 `02-report-with-sheet.hy.md` 的**源码视图**或 WYSIWYG 均可（命令改的是磁盘文件）：

1. `Ctrl+Shift+P` → **HyMD: 导出 Sheet Snapshot 到 assets**  
   - 若当前为内嵌 `data`，块体应变为 `snapshot: ./...univer.json`
2. `Ctrl+Shift+P` → **HyMD: 嵌入 Sheet Snapshot 到块体**  
   - 外置 JSON 读入块体 `data:`，`snapshot:` 行消失（assets 文件保留）

---

## 五、手动测试 — 路径 D：hymd-host 独立壳（M2）

### 构建与启动

```powershell
cd E:\BaiduSyncdisk\Code\CSharp\CursorProjects\HYmd
npm run build:host
.\apps\hymd-host\bin\Debug\net8.0-windows\hymd-host.exe
```

### 验收清单

| # | 操作 | 预期 |
|---|---|---|
| 1 | 文件 → 打开 `samples/02-report-with-sheet.hy.md` | Milkdown WYSIWYG + sheet 只读 Univer 预览 |
| 2 | 编辑正文 → Ctrl+S | 磁盘文件更新；标题栏 `*` 消失 |
| 3 | sheet 卡片 → **编辑** → 改单元格 → **保存** | `.assets/load-table.univer.json` 更新；公式仍在 |
| 4 | 关闭再打开同一文件 | 修改保留 |
| 5 | **AI 纯文本插入**：宿主开着文件，用记事本在文末追加 ` ```sheet id=ai-table` 块 | 编辑器自动刷新（或提示重新加载）；新块可编辑 |

### AI 插入 sheet 块示例（追加到 `.hy.md` 末尾）

```markdown

```sheet id=ai-table
rows: 10
cols: 5
```
```

---

## 六、手动测试 — 路径 B：HYmd Code（完整 IDE）

验证 fork 内置扩展与品牌壳。

### 前置（仅首次或依赖变更后）

```powershell
cd E:\BaiduSyncdisk\Code\CSharp\CursorProjects\HYmd
npm run build:vscode
npm run sync:fork

cd E:\BaiduSyncdisk\Code\CSharp\CursorProjects\hymd-code
$env:ELECTRON_MIRROR = "https://npmmirror.com/mirrors/electron/"
$env:NODEJS_ORG_MIRROR = "https://npmmirror.com/mirrors/node/"
npm ci          # 已成功则可跳过
npm run compile # 首次约 10–30 分钟
.\scripts\code.bat
```

> **注意**：`hymd-code` 体积大，**不要**长期放百度网盘同步目录内编译；`npm ci` 前建议暂停网盘同步。  
> 扩展激活问题：parser 已 **bundle 进 extension.js**，无需 `node_modules/@hymd/parser`。改扩展后务必重新 `sync:fork` 并重启 HYmd Code。

### 验收

与路径 A 相同清单；额外确认：

- 窗口标题/品牌为 **HYmd Code**（非 VS Code）
- 打开 `HYmd/samples/02-report-with-sheet.hy.md`（可用绝对路径）行为一致

---

## 七、手动测试 — 路径 C：纯 Markdown 降级（M0）

任意 Markdown 预览器（VS Code 内置预览、Typora、GitHub）打开 `samples/*.hy.md`：

- **正文**正常
- **扩展块**显示为围栏代码块（非交互表格）
- 内容仍人类可读（rows/cols/snapshot 路径可见）

---

## 八、常见问题

| 现象 | 处理 |
|---|---|
| 扩展激活失败 `No exports main @hymd/parser` | 在 HYmd 执行 `npm run build:vscode && npm run sync:fork`，重启 HYmd Code |
| sheet 预览空白 | 滚到卡片可见区域（懒加载）；看 DevTools 是否有 CSP/加载错误 |
| 保存后公式丢失 | 确认保存走覆盖层「保存」按钮；检查 JSON 是否写入 `.assets` |
| `npm ci` EBUSY | 暂停百度网盘；删 `hymd-code/remote/node_modules` 重试 |
| `npm run compile` getElectronVersion | 确认 `hymd-code/.npmrc` 含 `target="34.3.2"` 与 `ms_build_id` |
| F5 找不到扩展 | 打开的是 **HYmd** 仓库而非 hymd-code；先 `npm run build:vscode` |

---

## 九、相关文件速查

```
HYmd/
├── apps/hymd-host/              ← M2 WPF 独立壳
├── packages/hymd-webview-core/  ← 共享 Crepe + Sheet UI
├── packages/hymd-host-web/      ← WebView2 前端 dist
├── spec/hymd-spec-v0.1.md
├── samples/02-report-with-sheet.hy.md
├── samples/02-report-with-sheet.hy.assets/load-table.univer.json
├── packages/hymd-parser/
├── packages/hymd-blocks-sheet/
├── packages/hymd-vscode/
│   ├── .vscode/launch.json    ← F5
│   └── src-webview/sheetOverlay.ts
└── scripts/sync-to-fork.mjs

hymd-code/
├── README-HYMD.md               ← fork 构建
├── scripts/code.bat             ← 启动 HYmd Code
└── extensions/hymd-editor/      ← sync 目标
```

---

## 十、建议测试顺序（5 分钟版）

1. `npm test` — 确认 45 passed  
2. F5 打开 `02-report-with-sheet.hy.md` — sheet 预览 + 编辑保存  
3. 检查 assets JSON 公式 — 确认 roundtrip  
4. （可选）`sync:fork` + HYmd Code 复测同一文件
